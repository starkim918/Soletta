/// @description Insert description here
// You can write your code in this editor

/// @description Insert description here
// You can write your code in this editor

 

/*
if (global.game_state == game_states.PLAYING) {
    // fire bullets
    if (bullet_cooldown < 0) {
        var bullet;

        if (room == rm_level_1) {
            bullet = instance_create_layer(x,y-(sprite_height/2), "Instances", obj_bullet_player);
        } else {
            if (sprite_index == spr_player) {
                bullet = instance_create_layer(x,y-(sprite_height/2), "Instances", obj_bullet_player_blue);
            } else {
                bullet = instance_create_layer(x,y-(sprite_height/2), "Instances", obj_bullet_player_red);
            }
        }

 

        bullet.atk = atk;
        bullet.direction = point_direction(x,y,x,y-1); // straight up
        bullet.speed = bullet.spd;
        audio_play_sound(snd_shoot, 10, false);

        bullet_cooldown = 0.25*room_speed;
    } 
}

 

*/

